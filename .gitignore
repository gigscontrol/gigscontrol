node_modules/
.next/
out/
build/
dist/
.env*.local
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
.vercel
*.tsbuildinfo
next-env.d.ts
.claude/

# Scripts locais de manutenção (uso pontual via service_role; nunca devem
# ir pra produção, contêm operações destrutivas)
scripts/
gerar_contrato_dj.py

# Ferramentas/temporários locais — não vão pra produção
stripe.exe
_i18n_tmp/
*.log

# Downloads e material de referência que ficam na raiz (bundles baixados,
# prints mandados no WhatsApp). Nenhum .zip/.jpeg é versionado neste repo.
*.zip
*.jpeg
*.jpg

# Protótipos/preview que rodam só local. O onboarding-navegavel mora em
# public/, então commitá-lo o publicaria como página do site.
Gigs Control - Redesign (standalone).html
public/onboarding-navegavel.html
src/app/dev/
